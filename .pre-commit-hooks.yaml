- id: cfn-guard
  name: cfn-guard
  description: Wrapper around CloudFormation allowing pre-commit hooks
  entry: cfn-guard
  language: rust
  files: \.(json|yaml|yml|template)$
  args: [--fail-on-warnings, --input-path]
  require_serial: false